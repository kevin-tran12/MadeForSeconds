# Trivy vulnerability allowlist for the backend container image, enforced by
# .github/workflows/deploy.yml's "Scan image for HIGH/CRITICAL
# vulnerabilities" step (--severity HIGH,CRITICAL --exit-code 1).
#
# Every entry MUST carry an expiry date and a reason — an unreviewed
# permanent waiver is exactly how a real vulnerability stays unpatched
# forever. When an entry expires, either the fix has shipped upstream (drop
# the line) or it hasn't (open a fresh dated entry with updated
# justification — don't silently extend the old one).
#
# Format:
#   CVE-YYYY-NNNNN  # reason — added YYYY-MM-DD, expires YYYY-MM-DD
#
# Current entries, added 2026-09-01 while wiring up the gate for the first
# time (hardening plan PR 22): every one is a Debian OS package baked into
# python:3.12-slim with no fix published yet (empty "Fixed Version" in
# `trivy image` output — confirmed by actually running the scan, not
# assumed), and every affected package (perl, gzip, libacl1, libsqlite3-0,
# ncurses) is base-OS tooling this FastAPI app never shells out to or
# otherwise reaches at runtime — real exposure is materially lower than the
# CVSS score alone implies. Re-run `trivy image --severity HIGH,CRITICAL
# <image>` against a fresh build past the expiry date; drop whichever
# entries Debian has since fixed, re-date the rest with updated
# justification if they're somehow still open.
#
# A second, categorically different kind of entry starts below: a source
# package match. Debian builds many binary packages from one source package,
# and a CVE is filed against the source. Trivy therefore flags every binary
# package built from it, whether or not the defective program is one of them.
# libsystemd0 and libudev1 are built from the systemd source and ship exactly
# one shared library each (libsystemd.so.0, libudev.so.1); apt, coreutils,
# util-linux, bsdutils and libpam-modules link against them for journald
# support, which is the only reason they are in the image. The systemd
# binaries themselves — systemctl, systemd-homed, homectl, userdbctl, PID 1 —
# are not installed at all (`docker run <image> command -v ...` finds none of
# them, and there is no /sbin/init; the only other systemd content is a few
# inert unit files dropped by apt and util-linux with no service manager to
# read them). So a systemd CVE in a daemon we do not ship is a false positive
# here, not an accepted risk, and expect this to recur: every future systemd
# CVE will flag these same two libraries for as long as the base image ships
# them. Verify the claim before adding one — check which binary package the
# defect actually lives in, and confirm it is absent from the image — rather
# than assuming every systemd CVE is harmless.
CVE-2026-41992  # gzip — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-54369  # libacl1 — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2025-69720  # ncurses (libncursesw6/libtinfo6/ncurses-base/ncurses-bin) — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-11822  # libsqlite3-0 — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-11824  # libsqlite3-0 — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-13221  # perl-base — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-42496  # perl-base (perl-archive-tar) — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-8376   # perl-base — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-42497  # perl-base (perl-Archive-Tar) — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-48962  # perl-base (perl-IO-Compress) — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-57432  # perl-base — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-57433  # perl-base (Storable) — no fix published — added 2026-09-01, expires 2026-10-01
CVE-2026-9538   # perl-base (perl-Archive-Tar) — no fix published — added 2026-09-01, expires 2026-10-01
# Source-package match, not a reachable defect — see the note above.
CVE-2026-16742  # libsystemd0/libudev1 — the bug is in systemd-homed, which this image does not install; local privesc needing a homed-managed user, an unlocked home and a writable ~/.identity, none of which exist here — added 2026-09-03, expires 2026-10-01
#
# ── WARNING: accepted risk, re-check monthly ────────────────────────────────
# util-linux, added 2026-09-04 when a database update landed four mount/
# nsenter CVEs against source package util-linux 2.41.5-0+deb13u1 with no
# Debian fix published (empty "Fixed Version" in the deploy gate's own
# output, run 36). One source package, nine binary packages flagged:
# bsdutils, libblkid1, liblastlog2-2, libmount1, libsmartcols1, libuuid1,
# login, mount, util-linux.
#
# Why waived rather than fixed: no fixed version exists for trixie, and a
# base-image bump pulls the same packages. All four defects sit in the
# mount(8) / nsenter(1) privileged code paths — X-mount post-hooks and
# subdir resolution, restricted bind mounts, cgroup join — and each needs a
# caller able to mount or enter namespaces. In this image PID 1 is uvicorn
# as uid 1000, the container has no CAP_SYS_ADMIN, there is no fstab
# user-mount entry, and the app never shells out to util-linux; the
# libraries are present because apt and coreutils link against them. This
# is an accepted risk of the same class as the perl/gzip/ncurses entries,
# NOT a false positive like the systemd entry above.
#
# Re-check on or before the expiry, and whenever the base-image digest in
# backend/Dockerfile is bumped:
#   https://security-tracker.debian.org/tracker/source-package/util-linux
#   trivy image --severity HIGH,CRITICAL backend:<sha>   (without --ignorefile)
# If trixie has a fixed version, bump the base-image digest and DELETE these
# four lines — do not re-date them. If it still has none, open a fresh dated
# block with the tracker's current status; the expiry is the forcing function.
CVE-2026-76642  # util-linux (mount) — failed external mount helper still runs privileged X-mount post-hooks — no fix published — added 2026-09-04, expires 2026-10-01
CVE-2026-78408  # util-linux (nsenter) — --join-cgroup leaks root cgroup migration authority — no fix published — added 2026-09-04, expires 2026-10-01
CVE-2026-78409  # util-linux (mount) — X-mount.subdir detached-tree resolution can escape via intermediate symlinks — no fix published — added 2026-09-04, expires 2026-10-01
CVE-2026-78410  # util-linux (mount) — restricted bind mounts do not pin the source — no fix published — added 2026-09-04, expires 2026-10-01
