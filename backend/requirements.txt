# Upper bounds are deliberate: CI gates merges to main, so an upstream major
# release must never be able to turn the build red on its own. Raise a bound
# only alongside a run of the test suite against the new major.
fastapi>=0.116.0,<1.0.0
uvicorn[standard]==0.52.4
google-cloud-firestore==2.28.1
google-cloud-storage==3.13.1
google-cloud-logging==3.16.2
google-cloud-secret-manager==2.30.0
google-auth==2.56.3
pydantic-settings==2.15.0
python-multipart>=0.0.32,<1.0.0
redis>=8.1.0,<9.0.0
stripe>=8.0.0,<16.0.0
PyJWT[crypto]>=2.13.0,<3.0.0
reportlab>=5.0.1,<6.0.0
pyotp>=2.10.0,<3.0.0
qrcode[pil]>=8.2,<9.0.0
# Used directly by services/uploads.py's metadata stripper and its tests
# (PIL.Image, PIL.PngImagePlugin) — previously only pulled in transitively via
# qrcode[pil], which is fragile: a future qrcode release dropping or
# re-scoping its [pil] extra would silently remove a dependency this module
# actually imports.
Pillow>=12.3.0,<13.0.0
# mcp 2.x (2026-07-28 spec; stateless core, built-in OpenTelemetry spans). v1 is
# maintenance-only. Do not pin mcp-types separately; the SDK owns that version.
mcp[cli]>=2.1.1,<3.0.0
# Exports mcp's built-in spans to Cloud Trace (app/tracing.py). opentelemetry-api
# is already pulled in transitively by mcp; the sdk package must be compatible
# with whichever api version that resolves to, so let pip pick the matching
# release rather than pinning sdk on its own.
opentelemetry-sdk>=1.40.0,<2.0.0
opentelemetry-exporter-gcp-trace>=1.10.0,<2.0.0
# Sous Chef assistant (app/services/assistant.py). 1.x is built on httpx2, a
# separate package from the httpx pin below, so the two never conflict.
anthropic>=1.3.0,<2.0.0
pytest==9.1.1
httpx==0.28.1
pytest-asyncio==1.4.0
pytest-cov==6.0.0
