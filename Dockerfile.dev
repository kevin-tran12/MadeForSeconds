FROM node:22-alpine

WORKDIR /app

# Install dependencies first (layer caching)
COPY package.json package-lock.json ./
RUN npm install

# Copy the rest of the app
COPY . .

EXPOSE 5173

# Vite handles hot reloading via the volume mount in docker-compose
CMD ["npm", "run", "dev", "--", "--host"]
